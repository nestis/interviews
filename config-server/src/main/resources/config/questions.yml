server:
  port: 8082
endpoints:
  questions: /questions
  security: http://localhost:8090/auth/validToken
spring:
  resources:
    add-mappings: false
  data:
    mongodb:
      host: localhost
      port: 27017
      database: interviews